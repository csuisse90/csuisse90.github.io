The same three operations can sit on top of an array or a linked list. Code written against the operations does not change when the storage does — which is the whole reason for naming the abstract type separately.